# API Documentation

## Endpoints

- `GET /api/portfolio?address=<wallet>` — returns wallet portfolio data.
- `POST /api/analyze` — sends portfolio data to AI and returns analysis.
- `GET /api/privacy-status` — returns privacy contract status.
